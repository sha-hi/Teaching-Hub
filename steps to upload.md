# **First time git uploading....**

----------

cd "C:\\Users\\YourName\\Desktop\\your-folder-name"   ← (go to your folder)



(  



git init                                           ← (start git)

git add .                                          ← (add all files)

git commit -m "Initial commit"                     ← (save changes)

git branch -M main                                 ← (make sure branch name is main)

      git init && git add . && git commit -m "Initial commit" && git branch -M main         ++++++++++

git remote add origin https://github.com/sha-hi/Teaching-Hub.git  ← (connect to GitHub)

git push -u origin main                            ← (upload your files to GitHub)


git status && git add . && git commit -m "Gallery updated" && git push                  

=================================================================================================================

Every time after (new work)

---------------------------
git status 

git add .

git commit -m "Your message"

git pull --rebase origin main

git push origin main

git status && git add . && git commit -m "image corrected" && git push











